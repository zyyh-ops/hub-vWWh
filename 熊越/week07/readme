bert 文本分类和 实体识别有什么关系，分别使用什么loss？
 答：1.使用bert实现了多任务学习，使用的是交叉熵损失函数对句子和token的预测和实体识别，共享了同一个bert编码层
多任务训练  loss = seq_loss + token_loss 有什么坏处，如果存在训练不平衡的情况，如何处理？
 答： 训练不理想情况会出现2个极端，梯度出现2个方向，解决办法：loss = α * seq_loss + β * token_loss ，不同epoch动态调整权重。或者当训练数据足够时使用交叉训练方式

